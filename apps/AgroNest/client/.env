# Local development API (used by `npm run dev`).
# The variable name MUST be exactly VITE_API_URL (no spaces) or Vite ignores it
# and the app silently falls back to http://localhost:5001/api.
VITE_API_URL=http://localhost:5001/api
